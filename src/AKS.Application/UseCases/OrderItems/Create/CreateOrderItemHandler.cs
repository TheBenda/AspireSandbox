using AKS.Application.Repositories;
using AKS.Domain.Entities;

namespace AKS.Application.UseCases.OrderItems.Create;

public static class CreateOrderItemHandler
{
    // public static async Task<OrderItemCreated> Handle(CreateOrderItem request, IOrderWriteRepository orderWriteRepository, CancellationToken cancellationToken)
    // {
    //     var modifiedOrder = await orderWriteRepository.AddProductToOrderAsync(request.OrderId, request.ProductId, cancellationToken);
    //     return new OrderItemCreated();
    // }
}